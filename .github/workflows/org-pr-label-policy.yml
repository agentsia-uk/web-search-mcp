name: Organisation PR label policy

on:
  pull_request:
    types: [opened, reopened, synchronize, ready_for_review, labeled, unlabeled]

permissions:
  contents: read
  pull-requests: read

jobs:
  pr-label-policy:
    uses: agentsia-uk/ci-shared/.github/workflows/pr-label-policy.yml@c674dd2a837c3e1fbffd3d00bba7eaeeda52fc32
    permissions:
      contents: read
      pull-requests: read
